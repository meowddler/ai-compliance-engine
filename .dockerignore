# Anything not needed at runtime, and anything that must never enter an image.

.env
.git/
.github/
venv/
.venv/
__pycache__/
*.pyc
.pytest_cache/
.ruff_cache/

# Evidence is mounted at runtime, never baked in — it is customer data.
evidence_store/

# Development artifacts
compliance.db
notebooks/
benchmarks/
tests/
database/
docs/
*.md
!README.md